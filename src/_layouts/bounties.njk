---
layout: base.njk
section: projects
---
<div class="page-container">
  <h1>{{title}}</h1>
  <hr>
  {% if subtitle %}
    <p>{{ subtitle }}</p>
  {% endif %}

  {{ layoutContent | safe }}

  <p>We’ll publish the new bounty lineup here once the refreshed project roster is live.</p>
</div>
